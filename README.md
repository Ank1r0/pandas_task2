# pandas_task2
# LMS tasks are files with a names, task_np.ipynb and task_pd.ipynb
# BoM task is inside the file with a name BoM.ipynb

folder data is used to store csv files that are used as data source.
final_result with a datatime is a result of BoM task.
