# pandas_task2
# LMS tasks are files with a names, task_np.ipynb and task_pd.ipynb
# BoM task is inside the file with a name BoM.ipynb

File has inside some extra files that were inside the lectures and 2 csv files. first one inside the special_task/data/. This file has a dataset for a BoM tasks
The next file is final_result with a date, it's a final report for BoM task.